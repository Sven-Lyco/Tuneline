{
  "name": "tuneline",
  "private": true,
  "workspaces": ["client", "server", "shared"],
  "scripts": {
    "dev": "concurrently -n client,server -c cyan,yellow \"npm run dev -w @tuneline/client\" \"npm run dev -w @tuneline/server\"",
    "dev:client": "npm run dev -w @tuneline/client",
    "dev:server": "npm run dev -w @tuneline/server",
    "build": "npm run build -w @tuneline/client && npm run build -w @tuneline/server",
    "typecheck": "npm run typecheck -w @tuneline/client && npm run typecheck -w @tuneline/server",
    "lint": "npm run lint -w @tuneline/client",
    "format": "npm run format -w @tuneline/client"
  },
  "devDependencies": {
    "concurrently": "^9.1.2"
  }
}
